Python version used: Python 3.7

import the csv file people.csv in your IBM database name the table as 'people'

In connect.py:
1. replace the connection string with your own IBM database connection string.

In hello.py:
1. set the variable base_url with the public end-point of your IBM storage bucket

To run locally, run the following commands:

pip install -r requirements
python hello.py